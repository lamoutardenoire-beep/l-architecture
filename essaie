<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MIORA | Architecte de Systèmes</title>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;500;700&family=Syne:wght@700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg: #f4f1ea; /* Blanc cassé papier */
            --dark: #000000;
            --accent: #ff4d00; /* Un orange "Rebelle" pour les détails */
            --border-thin: 1px solid #000;
            --border-thick: 3px solid #000;
            --shadow: 8px 8px 0px #000;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            background-color: var(--bg); 
            color: var(--dark); 
            font-family: 'Space Grotesk', sans-serif; 
            overflow-x: hidden;
            padding: 20px;
        }

        /* --- STRUCTURE UNIQUE --- */
        .grid-container {
            display: grid;
            grid-template-columns: repeat(12, 1fr);
            gap: 20px;
            max-width: 1400px;
            margin: 0 auto;
        }

        /* --- HEADER / HERO --- */
        .hero-box {
            grid-column: 1 / 9;
            background: #fff;
            border: var(--border-thick);
            padding: 60px;
            box-shadow: var(--shadow);
            margin-bottom: 40px;
        }

        h1 { 
            font-family: 'Syne', sans-serif; 
            font-size: clamp(3rem, 8vw, 6rem); 
            line-height: 0.9; 
            text-transform: uppercase;
            margin-bottom: 30px;
        }

        .tagline {
            font-size: 1.5rem;
            max-width: 600px;
            font-weight: 400;
            border-left: 5px solid var(--dark);
            padding-left: 20px;
        }

        /* --- IMAGE ASYMÉTRIQUE --- */
        .image-box {
            grid-column: 9 / 13;
            border: var(--border-thick);
            height: fit-content;
            box-shadow: var(--shadow);
            background: #fff;
            padding: 10px;
        }
        .image-box img { width: 100%; height: auto; display: block; filter: grayscale(1); }

        /* --- SECTION "EST-CE TOI" (Version Cards décalées) --- */
        .section-title {
            grid-column: 1 / 13;
            font-family: 'Syne', sans-serif;
            font-size: 3rem;
            margin: 60px 0 30px;
            text-transform: uppercase;
        }

        .card {
            background: #fff;
            border: var(--border-thick);
            padding: 40px;
            box-shadow: 4px 4px 0px #000;
            transition: 0.3s;
        }
        .card:hover { transform: translate(-4px, -4px); box-shadow: 12px 12px 0px #000; }

        .card-1 { grid-column: 1 / 5; }
        .card-2 { grid-column: 5 / 9; transform: translateY(30px); }
        .card-3 { grid-column: 9 / 13; }

        /* --- L'OFFRE (Brutalist) --- */
        .offer-box {
            grid-column: 1 / 13;
            background: var(--dark);
            color: #fff;
            padding: 80px;
            margin-top: 100px;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
        }
        .offer-box h2 { font-family: 'Syne', sans-serif; font-size: 4rem; line-height: 1; }
        .offer-box .price { font-size: 6rem; font-weight: 800; color: var(--bg); }

        /* --- FORMULAIRE --- */
        .contact-section {
            grid-column: 3 / 11;
            margin-top: 100px;
            text-align: center;
        }
        input, textarea {
            width: 100%;
            background: transparent;
            border: var(--border-thick);
            padding: 20px;
            margin-bottom: 20px;
            font-family: inherit;
            font-size: 1.2rem;
        }
        .btn-cta {
            background: var(--dark);
            color: #fff;
            padding: 25px 50px;
            border: none;
            font-family: 'Syne', sans-serif;
            font-size: 1.5rem;
            text-transform: uppercase;
            cursor: pointer;
            transition: 0.3s;
        }
        .btn-cta:hover { background: var(--accent); color: #fff; }

        /* --- RESPONSIVE --- */
        @media (max-width: 900px) {
            .hero-box, .image-box, .card, .offer-box, .contact-section { grid-column: 1 / 13 !important; transform: none !important; }
            .offer-box { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <div class="grid-container">
        
        <!-- HERO -->
        <div class="hero-box">
            <h1>On arrête le cirque.</h1>
            <div class="tagline">
                Stabilisons ton revenu de coaching. Pas de tunnels compliqués. Juste des systèmes qui marchent.
            </div>
        </div>

        <div class="image-box">
            <img src="Cheval.png" alt="Miora Architecte">
        </div>

        <!-- PAIN POINTS -->
        <h2 class="section-title">Est-ce que c'est toi ?</h2>
        <div class="card card-1">
            <h3>Le Yo-Yo</h3>
            <p>Tes revenus sont imprévisibles et ton énergie s'épuise à essayer de tout contrôler.</p>
        </div>
        <div class="card card-2">
            <h3>Le Bricolage</h3>
            <p>Tu passes ton temps à "être présente" sans comprendre ce qui génère réellement des ventes.</p>
        </div>
        <div class="card card-3">
            <h3>La Fatigue</h3>
            <p>Tu veux un système simple que tu peux tenir même quand tu es débordée.</p>
        </div>

        <!-- OFFRE -->
        <div class="offer-box">
            <div>
                <h2>L'Accélérateur Simple</h2>
                <p>4 sessions intensives pour démanteler le superflu et reconstruire ta structure de vente.</p>
            </div>
            <div style="text-align: right;">
                <div class="price">800€</div>
                <p>Zéro bullshit. 100% clarté.</p>
            </div>
        </div>

        <!-- FORMULAIRE -->
        <div class="contact-section">
            <h2 style="font-family: 'Syne'; margin-bottom: 40px;">PRÊTE À BÂTIR ?</h2>
            <form>
                <input type="text" placeholder="Ton Prénom" required>
                <input type="email" placeholder="Ton Email" required>
                <textarea rows="4" placeholder="Où en est ton business aujourd'hui ?"></textarea>
                <button type="submit" class="btn-cta">Je réserve mon appel</button>
            </form>
        </div>

    </div>

    <footer style="margin-top: 100px; padding: 40px; border-top: var(--border-thick); text-align: center;">
        <p>© MIORA — REBELLE x HÉRO — ARCHITECTE DE SYSTÈMES</p>
    </footer>

</body>
</html>
